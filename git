# Créer les fichiers d'abord

cd le dossier

git init
git add .
git status
git config --global user.email "yotrack140@gmail.com" 
git config --global user.name "loki9t" 
git config --global --list
git commit -m "Création des fichiers"
git remote set-url origin https://github.com/loki9t/hitema
git remote -v
git push --set-upstream origin master

(git checkout -b master  # Crée et bascule sur master
git add .  # Ajoute tous les fichiers (si tu as du contenu à committer)
git commit -m "Initial commit")






Synchro du github vers le dossier 
git pull origin master


 227  git branch -M main
228  git push -u origin main

ANSIBLE.cfg
[defaults]
inventory=./inventory.ini

INVENTORY.INI
[gpdocker]
docker60

[gpdocker:vars]
ansible_python_interpreter = /usr/bin/python3


[gpnginx]
nginx01
nginx02
nginx03
[gpnginx:vars]
ansible_python_interpreter = /usr/bin/python3


PLAYBOOK-MACVLAN172
---
- name: Creation de MacVlan172
  hosts: gpdocker
  become: true
  user: root
  tasks:
  - name: Create macvlan network for containers
    docker_network:
      name: "macvlan_173"
      state: present
      driver: macvlan
      driver_options:
        parent: "ens33"
        ipam_config:
        - subnet: '172.19.20.0/24'
        gateway: '172.19.20.254'


/ETC/SAMBA/SMB.CONF
[global]
workgroup = promoresb60
server string = docker
map to guest = bad user
obey pam restrictions = yes
valid users = root, theo, luc
security = user
os level = 65
domain master = no
local master = yes

[partage]
comment = share directory
path = /tmp/projet-devops/
public = yes
writable = yes
read only = no
browseable = yes
printable = no




